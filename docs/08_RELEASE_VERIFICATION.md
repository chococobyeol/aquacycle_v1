# 08. 배포 검증 기록

> 갱신일: 2026-07-27
> 대상: 현재 작업본의 미션 1~7, 실험실, 생태·물질 순환 및 관찰 기능

이 문서는 과거 어느 시점의 통과 개수만 남기는 기록이 아니라, 현재 배포본에서
무엇을 실제 문제로 판정하는지와 같은 검사를 다시 실행하는 방법을 함께 고정한다.

> **현재 배포 판정:** 미션 1~7의 장기 생태 게이트와 데스크톱 Renderer
> 안정성 게이트를 분리한다. 큰물벼룩 탈피–육방과 체리새우 난소 주기를
> 적용한 두 미션 7 fixture는 제한시간·미션 성공 문자열을 사용하지 않은
> 장기 생태 기준으로 통과했다. 그러나 2026-07-27 실제 Electron 장시간
> 실행에서 Renderer 메모리 종료가 다시 확인됐으므로, 아래 수정은 새
> Renderer의 실제 장시간 실행을 통과하기 전까지 데스크톱 안정성
> **재검증 대기**로 기록한다.

## 1. 배포 전 일괄 검증

```text
npm run verify:release
```

이 명령은 다음 순서로 실행된다.

1. TypeScript 정적 검사
2. 현재 저장소에 등록된 전체 자동 회귀를 단일 작업자에서 실행
3. 미션 6 생산자·미생물·체리새우계의 낮·밤 50주기 확장 검증
4. 미션 7 최소 정상 배치와 풀스톡 배치의 장기 생태 행렬 검증

단일 작업자는 모델을 느슨하게 만드는 설정이 아니다. 서로 독립적인 장시간
시뮬레이션들이 같은 CPU를 동시에 차지해 성능 제한시간만 넘기는 거짓 실패를
막는다. 64배속 계산량과 Worker 성능 계약은 각각의 전용 시험에서 그대로 검사한다.

생애·먹이망 변경 중 미션 1~7 누적 장기 게이트만 다시 실행할 때는 다음
명령을 사용한다.

```text
npm run verify:ecology-long-run
```

## 2. 생태 검증 작성 규칙

- 과제 해법 검증은 목록에서 항목을 고르고 수조에 놓는 공개 명령만 사용한다.
- 정상 플레이에서 만들 수 없는 방식으로 모든 셀의 생체량을 직접 채우지 않는다.
- 광원 출력 숫자가 크다는 이유만으로 생산성이 높다고 가정하지 않는다. 종별
  광반응을 지난 실제 생산량·생체량·누적 섭식량을 관찰한다.
- 순간 개체 수 하나로 숨은 수용량을 추정하지 않는다. 출생 기록, 실제 섭식,
  세대교체와 기술 안전선이 생태 판정에 개입하지 않는지를 나눠 확인한다.
- 미션의 성공·실패, 제한시간, 목표 유지시간은 UI 과제 회귀에만 사용한다.
  장기 생태 판정은 성공하지 못한 실행도 끝까지 계속 돌려 같은 기준으로
  평가한다.
- 새우 개체군의 멸종과 탄소·질소·산소 등가 장부 누출은 별개로 판정한다.
- 미션 해법은 특정 장식 배치를 강요하지 않되, 공급된 먹이를 볼 수 있는 위치에
  배치하는 것처럼 플레이어가 화면에서 판단할 수 있는 행동으로 구성한다.

단위 수식 시험은 필요한 상태 하나만 고립하기 위해 내부 값을 설정할 수 있다.
그러나 그 결과를 그대로 과제의 플레이 가능성이나 전체 생태 평형의 증거로 쓰지
않는다.

### 2.1 미션 1~7 장기 생태 수락 행렬

미션 번호는 어떤 생태 구성요소가 처음 등장하는지 고르는 fixture 이름일
뿐이다. 미션 목표 문구를 검사하지 않고, 해당 시점까지 등장한 생물이 공개
배치 명령으로 만든 수조에서 장기적으로 기능하는지를 누적 검증한다.

| 범위 | 장기 판정 |
|---|---|
| 미션 1 | 붓뚜껑말 생체량이 유한·비음수이고 실제 광량에 반응하며, 장기 실행 뒤 0 또는 비유한값으로 무너지지 않음 |
| 미션 2 | 규조류에 같은 수치 안전성을 적용하고, 저광·적정광의 상대 반응이 유지됨 |
| 미션 3 | 구조물과 바닥의 붓뚜껑말 군락이 전체 자원 한도 안에서 유지되고 셀 수를 늘렸다고 물질이 무한 생성되지 않음 |
| 미션 4 | 두 부착조류와 체리새우가 함께 남고, 창시자 노화 뒤 출생 자손의 성숙·번식이 이어지며 후반 표본의 개체 수가 0이 아님 |
| 미션 5 | 미션 4 조건에 두 균 기능군, 유기물–암모니아성 노폐물–영양염 순환, 안전한 산소와 탄소·질소·산소 등가 보존을 추가 |
| 미션 6 | 미션 5 조건에 낮·밤 생산자 호흡 전환, 나사말 러너 후속 세대와 후반 나사말 생체량을 추가 |
| 미션 7 | 미션 6 조건에 식물플랑크톤의 고갈 뒤 회복, 큰물벼룩의 탈피·출생·성숙·창시자 교체와 살아 있는 후속 세대를 추가 |

개체군에는 특정 최종 마릿수나 평균값을 정답으로 두지 않는다. 종과 다음
먹이망 단계에 맞춘 후반 최저값, 후속 세대의 출생·성숙, 자원 고갈 뒤
회복과 비정상 폭증 경보를 함께 본다. 현재 미션 7 큰물벼룩 수락선은 후반
최저 `15마리`, 평균 `30마리`, 최종 `20마리` 이상이며 체리새우는 후반
모든 표본에서 `4마리` 이상이다. `19`와 `20`처럼 한 표본의 경계 차이를
생태식 보정 근거로 삼지 않되, 마지막 순간에만 수가 늘고 후반 출생·성숙이
끊긴 실행도 통과시키지 않는다.

## 3. 2026-07-24 오래된 회귀 계약 교정

| 이전 검사 | 문제 | 현재 검사 |
|---|---|---|
| 새우가 없는 수조보다 조류가 반드시 `8%` 이상 적어야 함 | 재성장이 섭식을 일부 상쇄하는데 임의의 순간 비율을 모델 정답으로 사용 | 실제 접종 수조에서 누적 섭식, 전체 생체량 감소와 한 셀 이상의 눈에 보이는 국소 감소를 함께 확인 |
| 모든 표면 셀을 생체량 `0.72`로 강제한 뒤 800초 안에 12마리를 넘어야 함 | 플레이어가 만들 수 없는 초기 상태이며 번식 시점과 성비를 숨은 상한으로 오판 | 실제 접종·배치 후 출생 기록, 초기 4마리 초과와 누적 섭식을 확인 |
| 전등 `80` 수조의 새우 수가 전등 `30` 수조보다 많아야 함 | 종별 과광 스트레스와 포식자–피식자 위상차를 무시 | 실제로 먹이를 기른 수조와 빈 수조를 비교해 섭식과 생존 차이를 확인 |
| 미션 6 새우를 군락과 무관한 고정 좌표에 놓음 | 국소 먹이 탐색 전에 우연히 굶는 결과가 해법 검증을 지배 | 실제 접종한 네 먹이 군락에 성체를 놓고, 시간차 균 접종 뒤 3주기 연속 생존·번식·장부 보존을 확인 |

## 4. 마지막 일괄 검증 기준점

아래 표는 `npm run verify:release`를 실제로 끝까지 실행한 시점의 기록이다.
이후의 전용 변경 검증 결과를 전체 일괄 회귀 통과 수에 합쳐 쓰지 않는다.

| 날짜 | 정적 검사 | 전체 회귀 | 50주기 확장 | 결과 |
|---|---:|---:|---:|---|
| 2026-07-24 | 통과 | `47`파일 · `246/246` 통과 | `8/8` 판정 통과 | 통과 |
| 2026-07-25 | 통과 | `53`파일 · `279/279` 통과 | `8/8` 판정 통과 | 통과 |
| 2026-07-26 | 통과 | `59`파일 · 등록 `336/336` 검증 완료¹ | 새우 포함 `14/14` 판정 통과 | 통과 |
| 2026-07-27 | 통과 | `60`파일 · `346/346` 단일 작업자 연속 통과 | 새우 포함 `14/14` 판정 통과 | 통과 |
| 2026-07-27 Renderer 수정 | 통과 | `64`파일 · `364/364` 단일 작업자 연속 통과 | 새우 포함 `14/14` 판정 통과 | 자동 검증 통과 · Electron soak 대기 |

¹ 전체 단일 작업자 실행에서는 `333/336`이 먼저 통과했고, 광량장·수온장
상태 검사 2개가 계산 완료 직후 벽시계 여유를 각각 약 `0.55/0.68초`
넘겼으며 주야간 10주기 검사는 초기·후속 창의 산소 평균 차이를 평형
실패로 보던 옛 조건 1개가 실패했다. 생태 식은 바꾸지 않고 상태 검사의
시간 여유만 조정하고, 주야간 검사는 안전 범위·세대교체를 직접 보도록
교정한 뒤 해당 `13/13`을 다시 통과했다. 따라서 한 번의 명령이 연속으로
`336/336`을 출력했다는 기록은 아니며, 등록된 각 검사가 현재 코드로
통과했음을 뜻한다.

2026-07-27 작업본은 별도 검사의 합산이 아니라 단일 작업자 전체 회귀 한
번에서 `60`파일의 `346/346`을 연속 통과했다. 새 먹이·교미 단서가 없는
수조는 비어 있는 단서장을 갱신하지 않으며, 전체 장주기 직후의 성능 검사도
광 경로 재사용 `4.42초`, 고배속 개체 모션 `15.70초`로 통과했다. 이 시간은
생태 합격식이 아니라 알고리즘 퇴행 경보다.

이번 Renderer 수정 뒤에도 한 번의 `npm run verify:release`에서 정적 검사,
`64`파일 `364/364`, 50주기 `14/14`, 미션 7 두 fixture `13/13`씩을
연속 통과했다. 이는 생태·전송·표시 코드의 자동 회귀 근거이며 아래 실제
Electron soak 대기 판정을 바꾸지는 않는다.

같은 날 사용자가 실행해 둔 개발용 Electron의 미션 7을 `×64`로
`700:00`까지 진행한 상태에서 Renderer가 다시 종료됐다. 종료 직전 생태는
물벼룩 약 `202`, 새우 `55`, 남은 사체 `29`, 식물플랑크톤 약 `17`이었다.
누적 출생 `6,595`, 사망 `6,345` 중 노화 `6,001`, 굶주림 `344`, 기타
사망 `0`이므로 수조 계산이 절멸·정지한 상태가 아니라 큰 세대교체로 사체가
몰린 상태였다.

직접 종료 원인은 Renderer 프로세스의 V8/backing-store·네이티브 메모리
구역 고갈이다. `renderer-health.log`의 PID `97669` 전용 메모리는 약
`6.68GB`까지 증가했지만 메인 창 JS heap은 대체로 `60~247MB`였다.
`2026-07-27T11:09:20.881Z`에
`renderer process gone: reason=crashed exitCode=5`가 기록됐고, macOS
진단 보고서는 `EXC_BREAKPOINT/SIGTRAP`, V8 `Memory Tag 253`
`42,848`개 구역과 writable virtual 약 `9.7GB`를 기록했다. 잠시 뒤 미션
목록이 보인 것은 복구가 아니라 Electron이 새 Renderer PID `98791`을
자동 생성한 결과다.

확인된 주요 증가 경로와 함께 발견한 정합성 결함은 다음처럼 분리한다.

- `stepGrowth`가 해당 종의 생체량이 없는 셀에서도 나사말 잎·수관 기하를
  계산하고 성장 전후 객체·모집 목록을 반복 생성했다.
- 식물플랑크톤과 부착조류 Canvas texture를 전체 스냅샷마다 다시 업로드했다.
- 집단 사망 때 생체 리그가 반환되기 전에 활동 사체 리그가 모두 추가되고,
  반환 풀도 과거 최고 개체 수를 크게 유지했다.
- 단일 공유 payload를 읽는 중 Worker가 같은 바이트를 덮을 수 있었고,
  고정 payload 초과는 UI 갱신을 조용히 중단했다. 이는 crash의 유일
  원인으로 단정하지 않지만 별도 정합성·정지 위험이다.

현재 수정은 빈 생산자 셀을 생리·수관 계산 전에 건너뛰고, 성장 작업장과
사체 aging을 제자리 재사용한다. 식물플랑크톤·부착조류는 정적 texture와
Sprite transform만 갱신하며, 활동 사체 트리는 종별 표시 상한을 사용한다.
전체 스냅샷 채널은 세 고정 슬롯과 읽기 슬롯 claim·슬롯별 길이를 사용하고,
용량 초과 시 일반 메시지로 복구한다. 복원 사체 값과 물벼룩 최대 표시 배율,
표시 사체와 선택 목록도 같은 경계로 정규화했다. 생체량·먹이·섭식·번식·
수명·사망과 사체 물질 장부는 바꾸지 않았다.

할당 표본에서 같은 미션 7 다섯 tick 구간은 약 `5.84초 → 2.82초 →
2.21초`로 줄었고, `stepGrowth` 표본 할당은 약 `85MB → 18.7MB`로
감소했다. 이는 확인된 고빈도 지배 경로가 제거됐다는 비교값이지 실제
Electron 장시간 합격을 대신하지 않는다. Node 기반 `18,003초` 생태
검증의 RSS나 메인 창 `performance.memory`만으로도 Renderer 합격을
선언하지 않는다.

### 4.1 장시간 Renderer 안정성 게이트

다음 실제 Electron 실행은 자동 생태 회귀와 별도로 모두 만족해야 한다.

- 새 Renderer PID에서 미션 7 `×64`를 최소 `45,000` 시뮬레이션초까지
  진행해 기존 `42,000초` 실패 지점을 넘긴다.
- 워밍업 뒤 1분 간격 `private` 메모리가 계속 직선·단조 증가하지 않는다.
  보수적 경보선은 연속 15분 증가량 `256MB`다.
- 같은 Renderer PID가 유지되고 `renderer process gone`이 없어야 한다.
- 진행 뒤 메뉴·일시정지·개체/사체 선택·관찰 패널이 즉시 반응해야 한다.
- 사체는 종별 표시 수명이 지나면 사라지고, 동시 사망 때 화면 트리는 종별
  표시 상한을 넘지 않아야 한다.
- 메인 창 heap, Node 생태 실행과 실제 Renderer soak는 서로 대체하지 않는다.

50주기 확장 검증의 현재 측정값은 다음과 같다.

| 항목 | 결과 | 합격 기준 |
|---|---:|---:|
| 시뮬레이션 시간 | `18,003초` · `50주기` | 50주기 이상 |
| 최저 평균 산소 | `75.5308` | 18 초과 |
| 최대 평균 유기물 | `12.1104` | 18 미만 |
| 후반 두 구간 평균 산소 차이 | `0.3866` | 6 미만 |
| 후반 두 구간 평균 유기물 차이 | `0.0073` | 1.5 미만 |
| 후반 25주기 새우 최저값 | `2마리` | 1마리 이상과 후반 출생·성숙 |
| 최종 새우 | `8마리` · 성체 암컷 `2` · 수컷 `3` · 어린 개체 `3` | 수조 출생 자손, 암수 계통과 성체 단계 유지 |
| 후반 새우 출생·성숙 | 둘 다 발생 | 둘 다 발생 |
| 질소 상대 장부 오차 | `-2.78×10⁻¹⁴` | 절댓값 0.00000001% 미만 |
| 탄소 상대 장부 오차 | `-3.30×10⁻¹⁴` | 절댓값 0.00000001% 미만 |
| 산소 등가 상대 장부 오차 | `-3.07×10⁻¹⁵` | 절댓값 0.00000001% 미만 |

전체 회귀는 미션 1~3의 생산자 수조를 제한시간 뒤 `1,800초`까지,
미션 4를 `3,600초`, 미션 5를 `7,200초`, 미션 6을 자동 10주기와 별도
50주기까지 진행한다. 미션 결과 문자열은 쓰지 않고 후반 절멸 없음,
수조 출생 자손의 출생·성숙, 나사말 러너, 두 균, 수질과 물질 장부를
누적 확인한다. 개편 미션 7은 두 공개 배치 fixture를 각각 `10,800초`
진행하며 물벼룩·새우·나사말의 후속 세대와 생산자 회복까지 함께 확인한다.
별도 대조 시험에서는 물벼룩이 부유 분해균을 실제로 여과하되 세균만으로는
세대를 만들 수 없고, 혼합 먹이에서는 장부를 누출하지 않음을 확인했다.

기존 송사리 시험은 지급 성체의 독립적인 나이·수명, 국소 어린 새우 포식,
포식 질량의 단일 이동, 구애·산란, 알 부착·부화와 저장·복원을 확인하는
코어 기능 시험으로 남아 있다. 부화한 치어의 다음 세대와 송사리–먹이군의
장기 공존은 큰 수조의 미션 8에서 다시 판정한다.

### 이전 생애 모델의 미션 5 장주기 개체군 결과

아래 값은 고정 휴지기 모델의 역사 기록이며 현재 합격 근거로 사용하지 않는다.
당시 `7,200초` 고정 시드에서 체리새우는 최소 `4`, 최대 `8`, 최종 `8마리`로
세대를 이어 갔다. 출생 `32`, 사망 `28`이며 사망 원인은 모두 노화였고,
저산소·굶주림 사망은 없었다. 암모니아성 노폐물은 `0.448~1.245`,
최종 `0.601`이었다. 이는 개체수 상한이나 미션 전용 번식률이 아니라 최근
실제 섭식 순잉여에 연결한 공통 번식 규칙의 결과다.

## 4.2 최신 미션 7·관찰 검증

기존 `4,800초` 검사는 마지막에 몇 마리가 남았는지만 확인해, 후반부에
실제로 출생·성숙이 멈춘 군집이나 먹이가 한 방향으로 감소하는 수조도
통과시킬 수 있었다. 한 가지 풀스톡 배치만 검사해도 적은 표면 먹이만 놓는
보통 플레이를 놓친다. 따라서 아래 개체·화면 단위 회귀는 유지하되,
생태계 배포 판정은 서로 다른 두 `10,800초` 시나리오를 같은 기준으로 검사한다.

| 범위 | 확인 결과 |
|---|---|
| 물벼룩 개체 모델 | 화면 한 마리와 계산 개체 한 마리가 1:1로 대응하고 식별자·위치·나이·수명·세대를 개별 추적 |
| 물질 규모 | 한 번 배치하면 게임 물질 `0.010575`인 성체 한 마리. 지급 체리새우 `1.08`의 약 `1/102`; 실제 건중량/습중량의 정밀 환산값은 아님 |
| 여과·번식 | 유생·성체에 같은 `M^0.75` 연속 체질량 여과 곡선을 적용하고, 모체가 실제로 먹어 유지비를 넘긴 고품질 먹이와 보존된 번식 질량만 출생에 사용 |
| 국소 이동 | 먹이 방향 레이더 없이 상관 배회·흐름으로 이동하고, 산소·독성 회피만 반경 `42`의 인접 8방향을 사용 |
| 압축 생애 | 수명 `750~1,550초`, 어린 개체별 `4~6회` 탈피 성숙, 성체 탈피 기준 `150초`, 배아 발생 `110초`; 탈피 때만 방출하며 체리새우보다 짧은 상대 관계 유지 |
| 옛 `4,800초` 지급 구성 | 물벼룩 `3→4마리`, 최대 `6마리`, 최고 `8세대`; 최종 생존만 확인했으므로 장기 배포 근거에서는 제외 |
| 부유 색 지도 | 첫 부유층은 연속 색면, 추가 부유층은 등치선·국소 지점; 식물플랑크톤 `12`, 부유 분해균 `8` 절대 스케일 |
| 관찰 기록 | 다섯 그래프 묶음을 독립적으로 접고 펼침; `수조 전체` 고정 중 선택 스냅샷이 탭·스크롤을 바꾸지 않음 |
| 카메라 | 맞춤 보기보다 `28%` 추가 축소, cover 기준 `50%` 하한과 패널 회피용 제한 이동 |
| 새우 섭식 | 조류·균 필름의 실제 잔량과 먹이별 가치로 요청량을 나누며 고정 최소 조류 몫은 없음. 실제 조류 감소분만 누적하고 세부 무늬도 생체량에 맞춰 갱신 |
| 새우 먹이 냄새 | 고정 확장 반경 없이 `36×20` 비물질 농도장이 기존 물 흐름·확산과 `45초` 반감기를 따름. 새우는 반경 `42`의 국소 농도/유속만 읽고 실제 먹이 셀은 반경 `64` 안에서만 선택 |
| 새우 교미 단서 | 준비된 비포란 암컷만 `18초` 반감기의 별도 국소 단서를 방출하고 준비된 수컷만 반경 `42`의 농도/유속을 읽음. 암컷의 직접 확인은 `140`, 실제 교미는 `36` 안에서 `3초`를 유지해야 성립 |

미션 7 장주기는 다음 명령으로 두 고정 fixture를 차례로 재현한다. 모델 수치
보정 중에는 `verify:mission-7-long-run:minimal`과
`verify:mission-7-long-run:full-stock`을 따로 실행해 어느 조건에서
실패했는지 분리한다.

```text
npm run verify:mission-7-matrix
```

두 fixture는 다음과 같다.

| fixture | 배치 | 놓치면 안 되는 문제 |
|---|---|---|
| `starter-only-minimal` | 낮은 둥근 돌 4개, 서로 떨어진 두 돌의 부착조류 각 1회, 중앙 틈과 양쪽 밝은 가장자리의 나사말 3포기, 두 먹이 구역에 암수 한 쌍씩 둔 새우 4마리, 식물플랑크톤·물벼룩 각 3개체, 두 균 기능군 각 2회 명시적 접종 | 숨은 초기 균막 없이 플레이어가 실제로 구성한 최소 순환계의 붕괴 |
| `full-stock-stress` | 낮은 둥근 돌 4개 각각에 두 부착조류, 중앙 틈과 양쪽 밝은 가장자리의 나사말 3포기, 두 구역에 암수 한 쌍씩 둔 새우 4마리, 식물플랑크톤·물벼룩 각 3개체, 두 균 기능군 각 2회 | 지급 자원을 많이 배치했을 때 생물 부하와 분해 부하가 커져 무너지는 문제 |

첫 구조 대조는 세로 판석 4개와 각 기둥에 한 마리씩 떨어뜨린 새우를
사용했다. 나사말 뿌리의 시작 광량이 `17.5~24.7`에 몰렸고 두 구성 모두
나사말이 사라졌다. 최소 구성은 조류 총량이 나중에 회복했는데도 새우가
`2,400초`에 절멸했고, 전체 지급 구성은 새우 `45마리`가 남았지만 후반
물벼룩 최저가 `10마리`였다. 탐색 반경을 넓혀 이 배치를 억지로 통과시키지
않고, 뿌리 사이에 밝은 틈을 남기는 낮은 둥근 돌과 먹이 접종점 옆의 암수
쌍으로 fixture를 바꿨다. 최종 장주기는 이 구조 효과와 공통 물 흐름을
따르는 새우 먹이·교미 단서 추적을 함께 측정하되, 두 단서장이 물질
장부를 바꾸지 않는 단위 대조도 별도로 실행한다.

두 시나리오는 모두 `10,800초`를 실행하고 후반 `7,200초`를 따로 판정한다.
최소 배치에만 느슨한 합격선을 주지 않는다.

- 물벼룩은 한 번 놓을 때 실제 한 개체만 생긴다. 자연번식 뒤 후반 최저
  `15마리`, 평균 `30마리`, 최종 `20마리` 이상과 후반 출생·성숙,
  창시자 교체와 살아 있는 2세대 이상 자손을 함께 확인한다. 이는 한 표본의
  `19/20` 차이를 절대 경계로 삼지 않으면서도 다음 단계에 넘길 수십 마리의
  먹이 저수량을 요구하는 통계 판정이며, 개체를 자동 보충하는 하한이 아니다.
- `1,000마리`는 게임 상한이나 번식 제한이 아니라 작은 미션 수조에서
  수천 마리로 치닫는 비정상 폭증만 잡는 검증 경보다. 관찰된 `211마리`
  파동은 정상 범위로 두며, 정상 계산에는 개체 수 하드 캡이나 자동 보충을
  두지 않는다.
- 체리새우 후반 출생·성숙, 살아 있는 출생 자손, 암수 계통과 적어도 한
  마리의 성체, 사망 원인
- 식물플랑크톤 바닥값뿐 아니라 의미 있는 고갈 뒤 회복
- 지급 나사말 소멸 뒤 러너의 러너까지 이어진 세대, 후반 생존과 최종 생체량
- 두 균 군집, 산소·독성 노폐물·유기물 안전 범위
- `120초` 표본 전체의 질소·탄소·산소 등가 최대 장부 오차

이 항목 중 하나라도 실패하면 화면상 미션 성공 여부와 무관하게 미션 7의
장기 먹이망은 미통과다. 이 밀도 검사는 물벼룩 자체의 장기 유지 조건이지
송사리 먹이 부하를 검증한 결과가 아니다. 큰 수조에서 송사리가 실제로
물벼룩을 포식하는 별도 부하 시험을 통과하기 전에는 `송사리 준비 완료`로
표시하지 않는다.

> **역사 기록(현재 판정에 사용 금지):** 이전 고정 휴지기 모델은
> `starter-only-minimal` 후반 `57~154마리`, `full-stock-stress`
> `50~124마리`, 전체 지급 `14,400초`에서 `50~125마리`를 기록했다.
> 이 값은 새 탈피–육방 모델의 예상 밀도나 합격선이 아니다. 새 작업본의
> 결과는 미션 1~7 누적 장기 게이트를 끝낸 뒤 별도로 기록한다.

> **현재 생애 모델 결과:** `starter-only-minimal`의 후반 물벼룩은
> `20~242마리`(평균 `130.23`, 최종 `195`), 새우는 최저 `10`, 최종
> `16`이었다. `full-stock-stress`의 후반 물벼룩은 `24~317마리`
> (평균 `138.07`, 최종 `317`), 새우는 최저 `25`, 최종 `64`였다.
> 두 실행 모두 후반 출생·성숙, 창시자 교체, 나사말 후속 세대,
> 식물플랑크톤 회복, 명시적으로 접종한 두 균, 수질과 물질 장부를
> 통과했다. 물벼룩의 수질·굶주림 사망은 없었고, 전체 실행의 최대 상대
> 장부 오차는 `3.27×10⁻¹⁴` 이하였다. 두 실행 모두 창시자는 `0`,
> 살아 있는 개체는 전부 후손이었고 최대 `11세대`까지 이어졌다.

## 4.2 2026-07-25 이전 송사리 미션 7 장주기 판정 철회

기존 성공 배치를 성공 시점 뒤에도 길게 실행하고 포식 압력과 보조식
이용률을 바꾸어 진단했다.

| 진단 | 결과 | 판정 |
|---|---|---|
| 송사리의 어린 새우 포식을 낮춤 | 새우는 남지만 송사리가 약 `3,000초` 전후 굶어 사라짐 | 송사리 먹이 기반 부족 |
| 어린 새우 포식을 높임 | 일부 배치에서 새우 파동이 커진 뒤 산소 부족과 연쇄 붕괴 | 작은 수조의 강한 직접 결합 |
| 부착조류 보조식을 높임 | 송사리와 체리새우의 역할이 겹치고 기존 조류 균형에 개입 | 생태 역할 분리 실패 |

따라서 `치어 1마리 부화`는 장기 먹이망 성공의 충분조건이 아니다. 미션 7은
식물플랑크톤과 물벼룩의 세대 연결로 개편하고, 송사리는 큰 수조와 물벼룩을
갖춘 다음 미션에서 다시 검증한다. 상세 내용은
[미션 07–08 상세 명세](./content/MISSIONS_07_08_PLANKTON_RICEFISH.md)를 따른다.

세부 과학적·수치적 근거는
[생태·수질·열수송 모델](./04_SIMULATION_MODEL.md), 장주기 물질 장부
수식과 기준은 [화학량론 장부 구현 기록](./06_STOICHIOMETRIC_LEDGER.md)을
따른다.

## 5. 2026-07-25 macOS 수동 화면 검증

자동 회귀와 별도로 실제 macOS 데스크톱 창에서 마우스로 조작하고 화면을
직접 확인했다. Windows 패키지는 이번 확인 범위에서 제외했다.

| 범위 | 실제 확인 내용 | 결과 |
|---|---|---|
| 메인·미션 목록 | 미션 1~7을 완료 여부와 관계없이 선택 가능, 완료 표식과 실험실 진입 표시 | 통과 |
| 미션 1 | 돌 꺼내기·낙하·안착, 편집 아이콘 정렬, 붓뚜껑말 접종, 64배속 성공, 성공 뒤 계속 관찰 | 통과 |
| 미션 2 | 규조류만 지급, 구조물 수량 표시, 총 규조류 양 판정 문구 | 통과 |
| 미션 3 | 붓뚜껑말 2회와 구조물 무제한 지급, 위치·방법을 제한하지 않는 총량 판정 | 통과 |
| 카메라 | 정보창을 연 채 61%까지 축소해 수조 전체 확인, 확대·화면 이동, 프레임·수면을 포함한 전체 보기 복귀 | 통과 |
| 미션 4 | 두 조류와 성체 4마리 실제 배치, 성공 후 계속 진행, 출생·사망·섭식 기록 갱신, 유한 배경 영양 자원으로 구조물 면적에 따른 먹이 무한 생성 방지 | 통과 |
| 미션 5 | 분해균/질산화균 배치용 지도 연동과 배치 후 복귀, 복수 색 지도 합성, 빈 선택 상태 보존 | 통과 |
| 미션 6 | 전등 없는 수면 전체 자연광, 낮 `92`에서 밤 `4`로 전환, 나사말 성장과 야간 호흡 정보 | 통과 |
| 미션 7 | `초록 물결을 따라서` 시작 설명, 큰물벼룩 1회 배치 시 개별 성체 1마리 생성, 빠른 좌표 전달 뒤에도 새우로 바뀌지 않는 종 식별, 벽 반사와 사체 침강 | 통과 |
| 실험실 편집 | 구조물 설치·회전·이동·삭제 UI, 삭제 후 화면 정리, `Escape` 선택 해제 | 통과 |
| 관찰 UI | 카드 분리, 드래그 이동, 크기 조절, 수조 기록으로 복귀, 패널과 범례 정렬 | 통과 |
| 냉동 수조 | 임시 수조 저장과 보관함 개수 갱신 확인 후 임시 항목 정리, 기존 저장 유지 | 통과 |

미션 2·3·5·6의 최종 성공 판정과 장주기 생존은 위 수동 확인에서 같은 계산을
반복하지 않고 전체 자동 회귀와 50주기 확장 검증 결과를 사용했다. 수동 확인은
지급 제한, 배치·선택·카메라·관찰·저장처럼 화면에서만 확인할 수 있는 계약에
집중했다. 이번 확인에서 새로 재현된 화면 파손, 흰 화면, 테두리 소실 또는
조작 불능 문제는 없었다.

기존 `5초` 제한의 나사말 수관 광량장 재사용 검사와 `15초` 제한의 수온 구배
검사는 전체 장주기 회귀 직후 CPU 상태에 따라 각각 약 `0.55/0.68초` 늦게
끝나 거짓 시간 초과가 발생했다. 두 검사는 벽시계 성능을 판정하지 않으므로
생태·수치 합격식은 바꾸지 않고 안전 시간만 각각 `15/30초`로 명시했다.
실제 64배속 성능 계약은 별도의 미션 4·5 장기 검사에서 계속 측정한다.

미션 4의 60분 자동 회귀는 평평한 바닥만 쓰지 않고 넓적한 돌과 세로 돌을 함께
배치하고, 바닥·돌 표면에 두 조류를 분산 접종한 뒤 성체 4마리로 시작한다.
후반 30분 전체에서 절멸이 없어야 하고 출생·성숙이 계속되며 종료 시 수조
출생 자손이 살아 있어야 한다. 새우 수는 임의의 생태 상한 대신 `2,048마리`
기술 안전선 접근 여부만 보고, 생산자는 유한 배경 자원의 최대 범위 안에
있는지를 확인한다. 별도 무조류 회귀는 초기 저장량만으로 미션 목표를
통과했는지가 아니라 실제 먹이 수조보다 개체군이 먼저 감소하는지를 확인한다.
